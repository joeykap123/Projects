import numpy as np


def MatrixMultiplicationMethod(matrix_a, matrix_b):
    if matrix_a is not np.matrix:
        Exception()
    if matrix_b is not np.matrix:
        Exception()
    matrix_c = np.matrix
    # The following triple-nested for-loop results in a matrix multiplication algorithm with cubic time complexity
    for i in range():
        for j in range():
            for k in range():
                return matrix_c
